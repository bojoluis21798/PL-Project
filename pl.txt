number x

job square outputs number using number x
    output x * x
end

x is square using (5) // assignment

square using (x) // square(5)

if x = "Hello" and y < 0 then
		
end

//assignment

x is 5
x is 5+5
x is square using (2) //assign with function 

//selection

if expr then //if statement
	body
end

if expr then //if-else statement
	body
else then
	body
end

if expr then //if-else if-else statement 
	body
orif expr then
	body
else then
	body
end

//iteration

//pre test

repeat while expr //while statement 
	body
end

foreach item in collection // for each
	body
end

// post test

do this //do while
	body
while expr

// data types

number // int, float, double 
word // string, char is length 1 string
flag // boolean

// arrays (all array types are dynamic)

numbers // array of int, float, double
words // array of strings
flags //array of boolean

numbers x is (1,2,3,4,5)
numbers x

3rd of x is 5

insert 5 into 3rd of x // push
attach 4 into x // push
remove 3rd of x // pop

//records

group shape contains // structure definition
	word color
	word type
end

shape x

shape groups x // define array of structures

color of x //accessing structures

//operators

//relational
	>, <, =

//logical
	not, or, and

//pre-defined function

print "Hello World"
lengthof using (arr)